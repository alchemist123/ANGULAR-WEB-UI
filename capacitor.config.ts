import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.steamcube.stemclass',
  appName: 'stemclass',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
